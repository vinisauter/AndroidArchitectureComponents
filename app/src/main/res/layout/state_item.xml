<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:android="http://schemas.android.com/apk/res/android">

    <data>

        <import type="android.view.View" />

        <variable
            name="adapterEvent"
            type="com.vas.architecture.github.repositories.presentation.components.RepoAdapter.AdapterEvent" />

        <variable
            name="state"
            type="androidx.lifecycle.State" />
    </data>

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical"
        android:padding="8dp">

        <TextView
            android:id="@+id/error_msg"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_gravity="center_horizontal"
            android:text="@{state.message}"
            android:visibility="@{state.error ? View.VISIBLE : View.GONE}" />

        <ProgressBar
            style="?android:attr/progressBarStyleHorizontal"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_gravity="center"
            android:visibility="@{state.loading ? View.VISIBLE: View.GONE}" />

        <Button
            android:id="@+id/retry_button"
            style="@style/Widget.AppCompat.Button.Colored"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_gravity="center"
            android:onClick="@{() -> adapterEvent.onQueryRetryRequest()}"
            android:text="@string/retry" />
    </LinearLayout>
</layout>